<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:security="http://www.springframework.org/schema/security"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
    http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-5.8.xsd">


    <!-- security-context.xml after -->
    <bean id="userServiceImpl" class="com.itwillbs.service.UserServiceImpl"/>

    <!-- security-context.xml before -->
    <bean id="customUserDetailsService" class="com.itwillbs.security.CustomUserDetailsService">
        <constructor-arg ref="userServiceImpl"/>
    </bean>

    <bean id="customAccessDenied" class="com.itwillbs.security.CustomAccessDeniedHandler"/>
    <bean id="customLoginSuccess" class="com.itwillbs.security.CustomLoginSuccessHandler"/>




    <!-- HTTP 보안 설정 -->
    <security:http>

        <!-- URL 접근 권한 설정 -->
        <!-- 나머지 모든 경로에 대해 접근 허용 -->
        <security:intercept-url pattern="/**" access="permitAll()"/>

        <!-- 특정 경로에 대해 접근 권한 설정 -->
        <security:intercept-url pattern="/admin/**" access="hasRole('admin')"/>

<!--        <security:intercept-url pattern="/sample/all" access="permitAll()"/>-->
<!--        <security:intercept-url pattern="/sample/member" access="hasRole('ROLE_MEMBER')"/>-->
<!--        <security:intercept-url pattern="/sample/admin" access="hasRole('ROLE_ADMIN')"/>-->

        <!-- 로그인 페이지 및 로그인 성공 핸들러 설정 -->
        <security:form-login login-page="/login"
                             authentication-success-handler-ref="customLoginSuccess"/>

        <!-- 접근 거부 처리 핸들러 설정 -->
        <security:access-denied-handler error-page="/login"/>

        <!-- 로그아웃 설정 -->
        <security:logout logout-success-url="/main" invalidate-session="true"/>
    </security:http>

    <!-- Authentication Manager 설정 -->
    <security:authentication-manager>
        <security:authentication-provider>
            <!-- CustomUserDetailsService를 사용하여 사용자 인증 -->
            <security:user-details-service ref="customUserDetailsService"/>
        </security:authentication-provider>
    </security:authentication-manager>


</beans>
